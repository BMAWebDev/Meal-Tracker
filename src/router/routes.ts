// models
import { IRoute } from "@/models/router";

// components
import { Home } from "@/screens/";

const routes: IRoute[] = [
  {
    name: "Home",
    component: Home,
    key: "Home-0",
  },
];

export default routes;
